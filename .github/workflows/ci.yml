name: Cloud-ByteXL CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flask flask-cors

      - name: Check Python syntax
        run: |
          python -m py_compile backend/app.py

      - name: Run tests
        run: |
          pip install pytest
          python -m pytest backend/tests/test_app.py

      - name: Build Docker image
        run: |
          docker build -t cloud-bytexl-backend .

      - name: Run Docker container
        run: |
          docker run -d --name cloud-bytexl-container -p 5000:5000 cloud-bytexl-backend
          sleep 5
          curl http://localhost:5000/

      - name: CI completed
        run: echo "Cloud-ByteXL CI pipeline completed successfully!"